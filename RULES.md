# Project Rules

- **Reference Only**: The 'pmp-ref/' directory is for reference purposes only. Do NOT modify, delete, or create any files inside this directory.
- **Environment Variables**: Sensitive keys are stored in '.env'. Do not commit '.env' to version control.
- **Workflow**: All new development for the AI-900 project should be done outside of 'pmp-ref/', following the 'docs/migration_plan.md'.